# Task-1
- I created a student data with the mark as the values
- Get the name from the user and check the user name wit the key in that data
- if match it show the value of the student name
- if not match the student name it show the student name is not found

  
# Task-2
- I created a  list with the values range from the 1 to 10
- declare the x variable , then using slice operation , x[:5] means starting from the 0 to 4 first five values it extract
- now reverse the extracted array x[::-1] it take entire range with -1 means last index 
- then print the orignal array and extracted and reverse extracted array
